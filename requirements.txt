# PyTorch — install with CUDA support from https://pytorch.org/get-started/locally/
# e.g.: pip install torch --index-url https://download.pytorch.org/whl/cu121
torch>=2.0.0
numpy>=1.24.0
scipy>=1.10.0
matplotlib>=3.7.0
tqdm>=4.65.0
# sEMG_sleeve only — XDF file reading
pyxdf>=1.16.5
